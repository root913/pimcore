name: 'Close stale issues'
on:
    workflow_dispatch:
    schedule:
        - cron: '37 7 * * *'

permissions:
  contents: read
  issues: write

jobs:
  php-cs-fixer:
    uses: pimcore/workflows-collection-public/.github/workflows/stale.yaml@main